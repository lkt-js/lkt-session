import { HasCookiesFallback } from "../value-objects/HasCookiesFallback";
export declare class Settings {
    static SUPPORTS_LOCAL_STORAGE: boolean;
    static SUPPORTS_SESSION_STORAGE: boolean;
    static COOKIE_FALLBACK: HasCookiesFallback;
}
